# 08 — Evaluation Log

Track every local evaluator run over time so regressions are visible immediately. Anchor metrics
per [00_problem_statement.md](00_problem_statement.md) Pillar IV: Hit Rate@K (coverage), MRR /
Top-K Hit Rate (precision), MTTC (efficiency), and the combined TechnicalScore the organizer's
evaluator reports.

| Date | Commit | Variant / change under test | Hit Rate@10 | MRR | MTTC | TechnicalScore | Notes |
|---|---|---|---|---|---|---|---|
| 2026-08-26 | (pending — recorded pre-commit) | baseline (organizer's weak BM25 starter, unmodified), `python -m evaluator.local_evaluator` over the 200 public dev sessions | 0.125 | 0.068034 | 9.81 | 0.10671 | Efficiency sub-metric: 0.119. Matches the repo's own documented baseline exactly (`docs/baseline_results.json`) — confirms catalog+sessions+starter+evaluator are wired correctly end to end. **This is our regression floor**: every future variant must be compared against these numbers. Per-scenario breakdown (boundary/browsing/buying/intent_override) is in `results.json` (gitignored, regenerate via the evaluator command above). |
| 2026-08-29 | `90d6501` | Phase 0 (steps 0.1-0.10), pre codex-review fixes | 0.425 | 0.226313 | 7.38 | 0.352794 | 3.3x the baseline TechnicalScore. Scenario: boundary 0.6/0.202/5.9, browsing 0.4875/0.293/7.04, buying 0.3625/0.166/7.55, intent_override 0.367/0.218/8.33. Retained only for before/after comparison — superseded by the fixed run below, which is what actually shipped. |
| 2026-08-29 | `aa41ca2` | Phase 0, post codex-review fixes (7 findings: rejection-decline misdetection, missing facets, budget/entropy cardinality dominance, cache-failure retry storm, override not resetting preference vectors, category comma-split mismatch, word-boundary matching) | 0.39 | 0.225595 | 7.715 | 0.328379 | **Honest finding, not hidden**: aggregate TechnicalScore *dropped* slightly vs. the buggy pre-fix run (0.328 vs 0.353) — still 3.1x the baseline and clears both Phase 0's floor and stretch pre-registered targets (`10_PRE_REGISTRATION.md`), so this doesn't block Phase 0 exit. Scenario: boundary 0.5/0.182/6.8, browsing 0.45/0.297/7.31, **buying 0.3125/0.163/8.09 (regressed from 0.3625/0.166/7.55)**, **intent_override 0.4/0.216/8.1 (improved from 0.367/0.218/8.33, as expected — the preference-vector-reset fix targets exactly this scenario)**. The buying regression is real signal, not fixed-code-is-worse: all 7 fixes address genuine correctness bugs (verified independently, not just by their score effect) — shipping the buggy behavior because it happened to score higher on this one 200-sample dev set would be reward-hacking the wrong target, and could easily generalize worse on the private 800-session set's different products. **Flagged as an explicit Phase 1.3 calibration target**: buying-track question-selection (which facets get asked, in what order) likely needs retuning now that the facet/entropy fixes changed what's even askable — this is exactly the kind of thing Phase 1.3's threshold sweep exists to catch and correct, not a sign the fixes were wrong. |

| 2026-08-29 | `7830323` | Phase 1.3 calibration, training split (n=160, held out from validation) | 0.48125 | 0.270828 | 7.006 | 0.401748 | Raised `overgenerality.MAX_DISTINCT_VALUES` 8→15 and added a distinct-value/pool-size ratio gate (≤0.5) so near-unique facets are excluded regardless of absolute pool size (codex review caught the first fix not scaling down). Scenario: boundary 0.625/0.325/5.5, browsing 0.603/0.352/6.24, buying 0.391/0.210/7.39 (recovering from Phase 0's regressed 0.3125), intent_override 0.36/0.204/8.44. |
| 2026-08-29 | `7830323` | Phase 1.3 validation confirmation (n=40, held out, never touched by tuning) | 0.45 | 0.283472 | 7.4 | 0.382042 | Consistent with the training-split number (0.382 vs 0.402) — no severe overfitting to the sweep. Small per-scenario samples (n=16 buying, n=5 override) are noisy; the full-200 confirmatory run below is the real exit number. |
| 2026-08-29 | `7830323` | **Phase 1 exit (full 200 sessions, locked configuration)** | **0.485** | **0.286714** | **6.99** | **0.408714** | **+24.5% TechnicalScore vs. Phase 0's exit number (0.328379 → 0.408714).** Every scenario improved with no regressions: boundary 0.6/0.39/6.1 (was 0.5/0.182/6.8), browsing 0.6125/0.368/6.11 (was 0.45/0.297/7.31), **buying 0.3625/0.207/7.66 (fully recovered to Phase 0's pre-fix number of 0.3625, this time with none of the underlying bugs)**, intent_override 0.4333/0.248/7.83 (was 0.4/0.216/8.1). This is the real Phase 1 exit number, comparable apples-to-apples with Phase 0's (both full 200 sessions). |

| 2026-08-30 | `9a9659e` | Phase 2.3 ablation, validation split (n=40): retriever-disagreement VoI signal | ON: 0.475/0.297/7.225/**0.402** vs OFF: 0.475/0.274/7.325/**0.393** | — | — | — | KEPT (enabled by default). Modest but consistent win: HitRate@10 tied, MRR/MTTC/TechnicalScore all favor ON. |
| 2026-08-30 | `9a9659e` | Phase 2.1 ablation, validation split (n=40), same commit: K=2 multi-interest vs K=1 | K=1: 0.45/0.298/7.6/**0.382** vs K=2: 0.425/0.283/7.625/**0.365** | — | — | — | DISABLED. K=1 wins on every metric — per the explicit rule ("if K=1 ties or wins, don't ship K>1"), matches D3's a priori risk assessment exactly. |
| 2026-08-30 | `9a9659e` | Phase 2.2 ablation, validation split (n=40), same commit: contextual bandit | OFF: 0.475/0.297/7.225/**0.402** vs ON: 0.425/0.291/7.525/**0.369** | — | — | — | DISABLED. OFF wins on every metric — matches D11's a priori cold-start-risk assessment (only ~3-6 real decisions/session). |
| 2026-08-30 | `9a9659e` | **Phase 2 exit (full 200 sessions, locked config: VoI kept, multi-interest + bandit cut)** | **0.49** | **0.286552** | **6.995** | **0.411066** | +0.6% TechnicalScore vs. Phase 1's 0.408714 — a modest gain, consistent with the one kept feature (VoI) itself being a modest win. Scenario: boundary 0.6/0.395/6.1, browsing 0.6375/0.382/6.08 (both up), buying 0.375/0.205/7.53 (up), **intent_override 0.367/0.213/8.33 (down from Phase 1's 0.433/0.248/7.83)** — flagged, not chased further given the small sample (n=30, ±3.3pp/session) and the strong net positive across the other three scenarios; worth a look in a future calibration pass if time allows, not blocking. **SUPERSEDED by the corrected row below** — measured before a hash-seed nondeterminism bug (see 2026-08-30 Phase 3.1 rows) was found and fixed; VoI's reported win here does not survive the fix. |
| 2026-08-30 | (Phase 3.1, pre-fix) | **Reproducibility bug found**: identical baseline config, identical TRAINING split (n=160), run twice (once solo while timing a run, once inside the round-1 tuning sweep) | run 1 (solo): 0.48125/0.28095/6.99375/**0.405035** vs run 2 (sweep, same config): 0.5/0.278832/6.8438/**0.416775** | — | — | — | Same code, same config, same 160 sessions, two different results — first sign of the bug. Follow-up on the faster validation split (n=40) confirmed it: run 1 partial (color/material gazetteer fix only applied) gave 0.363217, run 2 (same partial-fix state) gave 0.382717 — still non-reproducible, meaning the gazetteer fix alone wasn't the whole story. Root cause: `PYTHONHASHSEED` randomizes Python `set` iteration order per process; `catalog.py`'s `bm25_search`/`metadata_rank` and the gazetteer's colors/materials lookups all relied on it without realizing it. Fixed at the source (sorted iteration everywhere it mattered, not an environment-variable workaround) — see `wiki/03_design_log.md`'s full writeup. |
| 2026-08-30 | (post-fix, pre-closeout) | Determinism verification: identical baseline config, validation split (n=40), run twice after the full fix | both runs: 0.45/0.269792/7.5/**0.375938** (byte-identical) | — | — | — | Confirms the fix: two runs of the identical config now produce identical output to every decimal place. |
| 2026-08-30 | (post-fix) | **Phase 2 re-ablation, VoI ON vs OFF, both splits** (n=40 validation + n=160 training = 200 sessions) | ON and OFF byte-identical on BOTH splits: validation 0.375938/0.375938; training 0.417604/0.417604 | — | — | — | The originally-reported "modest win" (0.4022 vs 0.3933) does not survive the determinism fix — zero measurable effect across the entire 200-session dev set. **REVERSED: VoI cut** (`phase2/voi.py`, `USE_DISAGREEMENT_SIGNAL = False`). |
| 2026-08-30 | (post-fix) | Phase 2 re-ablation, multi-interest K=2 ON, validation split (n=40) | 0.375938 (byte-identical to K=1's shipped-default number on the same split) | — | — | — | K=2 never differs from K=1 on this data — `SPAWN_THRESHOLD=0.35` is never crossed by any session's positive-signal turns (consistent with D3's a priori read: one coherent hidden target per session, not competing interests). Verdict unchanged: **CUT**, now for a cleaner reason than the original (noise-driven) "K=1 wins" report. |
| 2026-08-30 | (post-fix) | Phase 2 re-ablation, contextual bandit ON, validation split (n=40), full determinism fix | ON: 0.45/0.26503/7.4/**0.376509** vs OFF (shipped default): 0.45/0.269792/7.5/**0.375938** | — | — | — | Gap shrank from the previously-reported large margins (0.359 or 0.369 vs 0.402) to a negligible, mixed-sign 0.0006 difference. Verdict unchanged: **CUT** — no measurement of this mechanism across three attempts has shown a real win; D11's a priori cold-start-risk case stands regardless. |
| 2026-08-30 | `7be9ba7` | **Corrected Phase 2 exit (full 200 sessions, all three Phase 2 items disabled, deterministic code) — supersedes the `9a9659e` row above** | **0.49** | **0.278234** | **6.96** | **0.40927** | Confirmed byte-identical across two full runs. Effectively unchanged from the original buggy-measurement number (0.411066) at this sample size — large-n averaging diluted the bug's impact on the *aggregate* number even though it was large enough to flip conclusions at n=40. The real correction is qualitative: Phase 2 is now honestly "tried three things, none earned their keep, net neutral vs. Phase 1" rather than "one small win" — see `wiki/03_design_log.md` for the full writeup. |
| 2026-08-30 | (post-`7be9ba7`) | **Phase 3.1 systematic threshold sweep**, `tools/tune_strategy.py`, `should_clarify`'s three knobs, training split (n=160) + validation split (n=40) | see notes — every candidate in the searched range matched the baseline exactly | — | — | — | `CLARIFY_BASE_LOW` (baseline 0.3): tested 0.01/0.2/0.3/0.4/0.5/0.7 — **all byte-identical** (0.417604 training / 0.375938 validation); 0.99 collapses to 0.188 (training) — catastrophic, confirms the mechanism responds, just not in the searched mid-range. `CLARIFY_MIN_POOL_TO_BOTHER` (baseline 4): tested 1 and 15 — both byte-identical to baseline. `CLARIFY_NO_ASK_AFTER_TURN` (baseline 7): tested 10 — byte-identical; tested 2 — collapses to 0.21875 (too little room to clarify before forced commit). **Conclusion: all three thresholds are robust across a wide practical range, with a wide safety margin before degradation** — no candidate beat the hand-set Phase 0/1.3 defaults on the held-out split, so per Ablation 3's decision rule (keep only if "after" beats "before" on held-out data) the defaults are UNCHANGED. A legitimate, systematically-verified "searched, no improvement found" result, not a wasted effort — it confirms Phase 1.3's manual calibration already found a robust operating point. |
| 2026-08-30 | (post-`9a9659e`, pre-closeout-commit) | Phase 2 codex review fix: bandit re-ablation, validation split (n=40), reward-tracking bugs fixed (uncapped pool signal + consume-once outcome) | ON: 0.425/0.261558/7.575/**0.359467** vs OFF (unchanged, no-op when disabled): 0.475/0.297/7.225/**0.402** | — | — | — | Re-run specifically because findings 1-2 below meant the original ON number (0.3693) was measured with a capped, often pool-size-invariant reward signal. With the bug fixed, ON got **worse** (0.359 vs the buggy run's 0.369), not better — confirms the "cut" verdict was correct on the merits, not an artifact of broken measurement. Bandit stays DISABLED; see `wiki/03_design_log.md`'s 2026-08-30 entry and `wiki/reviews/phase2-2.1-2.3-2026-08-30.raw.txt` for the full 3-finding triage. |

| 2026-08-30 | (Phase 3.5) | **Ablation 4: LLM listwise reranker (Claude Haiku 4.5), validation split (n=40)** — real user-provided `ANTHROPIC_API_KEY`, gated behind the cross-encoder's own margin-skip (only fires on genuinely ambiguous shortlists) | ON: 0.475/0.306806/7.325/**0.403042** vs OFF: 0.45/0.269792/7.5/**0.375938** | — | — | — | Consistent win on every metric — HitRate@10 +5.6%, MRR +13.7% (largest gain, as expected for a reranking mechanism), MTTC improved too. Gains concentrated in `browsing`; `buying`/`intent_override` unchanged at this sample size (too few ambiguous turns in n=16/5 to engage the booster). |
| 2026-08-30 | (Phase 3.5) | **Ablation 4 confirmatory, training split (n=160)** | ON: 0.51875/0.320578/6.66875/**0.442173** vs OFF: 0.5/0.280345/6.825/**0.417604** | — | — | — | Same consistent win, larger and now spread across **every** scenario including buying (+3.8% hit rate, +18% MRR) and intent_override (both metrics up) — the larger sample gave enough ambiguous turns to engage the booster broadly. **ENABLED by default** (`ranker.ENABLE_LLM_BOOSTER = True`) per Ablation 4's decision rule. |
| 2026-08-30 | (Phase 3.5) | **Full 200-session run with LLM booster enabled** | **0.51** | **0.321032** | **6.8** | **0.43531** | +6.4% TechnicalScore over the guaranteed cross-encoder-only baseline (0.40927), consistent with both splits' ablation results — every scenario improved (boundary 0.6/0.36/6.1, browsing 0.6625/0.442272/5.7875, buying 0.3875/0.231384/7.4125, intent_override 0.4/0.223796/8.1, all up from the no-booster run). **Caveat, not a formality**: this number describes an optional ceiling reachable only when `ANTHROPIC_API_KEY` is present. The organizer provides no hosted credentials for official grading, so the realistic expected submission score remains the guaranteed 0.40927. Report both numbers in any writeup; never present the boosted one as "the" score. |
| 2026-08-30 | (Phase 3.5) | **Slate hedging ablation** (`phase2/slate_hedging.py`), LLM booster OFF to isolate the effect — motivated by a diagnostic (`tools/calibration_check.py`): sessions reaching a genuinely FORCED commit are ~100% high-entropy (0.7-1.0) with a 2.97% hit rate | validation (n=40): ON 0.376071/0.45/0.270238/7.5 vs OFF 0.375938/0.45/0.269792/7.5 (essentially flat — too few forced-commit sessions at this sample size); training (n=160, confirmatory): ON **0.425645**/0.5125/0.281734/6.75625 vs OFF 0.417604/0.5/0.280345/6.825 | — | — | — | Real, modest win on the larger split (+1.9% TechnicalScore, +2.5% HitRate@10), zero regressions, gains concentrated in `buying` (+7.7% hit rate) exactly matching the calibration finding's prediction. **ENABLED by default** (`ENABLE_SLATE_HEDGING = True`) — this is on the GUARANTEED path (no API key needed), so it directly raises the expected competition score. |
| 2026-08-30 | (Phase 3.5) | **Corrected guaranteed-path full 200-session exit** (LLM booster OFF, slate hedging ON) — supersedes the `TechnicalScore 0.40927` guaranteed number above | **0.5** | **0.279435** | **6.905** | **0.415731** | +1.6% over the prior guaranteed baseline, confirming the training-split ablation held at full scale. **This is the new realistic expected competition score.** |
| 2026-08-30 | (Phase 3.5) | **Query-vector nudge ablation** (`phase2/query_nudge.py`, user-suggested: nudge the dense retrieval query embedding itself toward accumulated positive preference, not just post-hoc reranking), LLM booster OFF, slate hedging ON | validation (n=40): ON 0.377333/0.45/0.269444/7.425 vs OFF 0.376071/0.45/0.270238/7.5 (a wash); training (n=160, confirmatory): ON 0.415642/0.5/0.27589/6.85625 vs OFF **0.425645**/0.5125/0.281734/6.75625 | — | — | — | Consistent REGRESSION on the larger split (every metric down; browsing and buying both worse). Plausible cause: nudging only the dense leg away from the literal current-turn text reduces the BM25/dense complementarity RRF fusion relies on, rather than adding new recall. **CUT** (`ENABLE_QUERY_VECTOR_NUDGE = False`) — a well-motivated idea that didn't earn its keep on this data. |
| 2026-08-30 | (Phase 3.5) | **Full 200-session run, everything enabled** (LLM booster + slate hedging; query-vector nudge stays cut) | **0.515** | **0.321329** | **6.78** | **0.438299** | Confirms the two kept mechanisms compose without conflict — a further small lift over the LLM-booster-alone number (0.43531). Same caveat as above: this ceiling number requires `ANTHROPIC_API_KEY`; **0.415731 remains the realistic expected competition score.** |
| 2026-08-30 | (Phase 3.5, investigation) | **`decide_rerank_depth` cap raised 50→60** (`orchestrator.py`) to match `k_pool`, guaranteed path (LLM booster OFF, slate hedging ON), training split | ON (raised cap): 0.46875/0.260675/7.075/**0.391078** vs OFF (cap at 50, the current shipped value): **0.425645**/0.5125/0.281734/6.75625 | — | — | — | **Reverted, not shipped.** Motivation looked airtight on paper (candidates ranked 51-60 were fetched into the pool but silently dropped before reaching `rerank()`/`hedge_slate()`, never eligible for recommendation — pure waste, since latency isn't scored). Measured anyway per this project's ablation discipline, and it REGRESSED sharply (browsing hit rate 0.634921→0.52381) — widening the reranked pool apparently dilutes signal for the cross-encoder, giving it more chances to misrank a genuinely weaker candidate above the true target, rather than recovering "wasted" recall. A concrete reminder that mechanical-looking fixes still need empirical verification. Cap left at 50. |
| 2026-08-30 | (Phase 3.5, investigation) | **Weighted RRF fusion — `METADATA_RRF_WEIGHT` sweep** (new `reciprocal_rank_fusion(weights=...)` param), guaranteed path, training split (n=160) | 0.0: **0.433666**/0.5188/0.293470/6.6875; 0.5: 0.426991/0.5125/0.286637/6.7625; 1.0 (baseline): 0.425645/0.5125/0.281734/6.7562; 2.0: 0.367074/0.4313/0.261081/7.3438; 3.0: 0.329179/0.3812/0.243929/7.7313 | — | — | — | A clean, monotonic training-split trend: lower metadata weight = better, weight=0.0 (drop the leg entirely) best, weights above 1.0 regress sharply. **Confirmatory check on the held-out validation split contradicted it**: weight=0.0 gave 0.425/0.269444/7.6/**0.361333** vs baseline 0.45/0.270238/7.5/**0.376071** — WORSE, the opposite direction. Per `10_PRE_REGISTRATION.md`/Ablation 3's explicit rule ("a win only on the training split is meaningless by construction"), **declined** — a textbook overfitting-to-training-split trap the pre-registration's split discipline exists to catch. `METADATA_RRF_WEIGHT` left at its default 1.0 (unweighted RRF, unchanged behavior), but kept as a tunable knob (`strategy_config.py`) for a future session to investigate further (e.g. scenario-specific weighting) rather than a single global constant. |

| 2026-08-30 | (Phase 4) | **1-step lookahead question selection** (`phase2/lookahead.py`, expected score-entropy reduction per facet), guaranteed path, both splits | validation (n=40): ON 0.45/0.269792/7.9/**0.367938** vs OFF 0.45/0.270238/7.5/**0.376071**; training (n=160, confirmatory): ON 0.51875/0.287636/6.975/**0.426166** vs OFF 0.5125/0.281734/6.7562/**0.425645** | — | — | — | Validation regressed (MTTC 7.9 vs 7.5 — more turns needed); training essentially flat (+0.1%, MTTC still slightly worse). Per Phase 4's own mandatory higher-bar gate ("diminishing returns are likely by construction over an already-good 1-step heuristic"), this doesn't clear it. **DECLINED** — the existing entropy-based heuristic (`overgenerality.py`) was already good enough that a more theoretically-principled lookahead doesn't earn a measurable improvement. Phase 4's 2-step extension and the full counterfactual/synthetic rollout augmentation were not attempted given the 1-step result. |

| 2026-08-30 | (Phase 5.2) | **Offline reproducibility check**: `submission/` bundle copied to an isolated temp directory, `HF_HUB_OFFLINE=1`, empty `HF_HOME` (no access to this machine's real HF cache) | — | — | — | — | `Agent` constructed in 11.5s (catalog embedding cache hit) and produced a correct, well-formed turn-1 response using only bundled models/cache, zero network access. Confirms the submission is genuinely self-contained, not just "works because this machine already has the models cached." **SUPERSEDED below** — this test happened to `cd` into `submission/` first, which accidentally masked a real cache-path bug (see the recovered-reviews entry in `wiki/03_design_log.md`); it wasn't actually testing what it claimed to. |
| 2026-08-30 | (recovered review triage) | **6 "failed" codex reviews recovered from session transcripts, 9 findings triaged** — see `wiki/03_design_log.md`'s full account | — | — | — | — | Real bugs found: `tune_strategy.py` allowed multi-candidate validation-split contamination (fixed, structurally prevented now); Anthropic client had no bounded timeout (fixed, 8s/0 retries); the LLM booster's first "determinism fix" (`temperature=0`) was itself broken (unsupported SDK kwarg, crashed every call, silently disabled the booster) and has been properly fixed; weighted-RRF didn't skip zero-weight legs (fixed, affects only an already-declined experiment); **slate hedging shipped despite a validation-split wash, reversed to disabled** (see next row); `calibration_check.py` conflated "both" with "commit" (fixed); the embedding cache resolved relative to process cwd, not the package -- a real submission-breaking bug, now fixed and re-verified without the accidental cwd coincidence that had masked it; `build_submission.py` only warned (not failed) on a missing cache (fixed); Phase 4's lookahead had a probability-normalization bug (fixed, affects only already-disabled code). |
| 2026-08-30 | (corrected) | **Slate hedging REVERSED to disabled** (`ENABLE_SLATE_HEDGING = False`) — per the recovered review's finding that its validation-split result was a genuine wash (HitRate@10 identical, 0.45 both ON/OFF), not a confirmed win, and `10_PRE_REGISTRATION.md`'s own rule requires the held-out split to confirm, not just the training split | — | — | — | — | The earlier "guaranteed-path score raised to 0.415731" claim is retracted — see the corrected full-200 row below. |
| 2026-08-30 | (corrected) | **LLM booster re-ablated with the actually-working fix** (unsupported `temperature` kwarg removed, bounded timeout kept) | validation (n=40): ON 0.45/0.303681/7.4/**0.388104** vs OFF 0.425/0.266667/7.6/**0.3605**; training (n=160, confirmatory): ON 0.51875/0.321029/6.6625/**0.442434** vs OFF 0.5/0.280952/6.81875/**0.417911** | — | — | — | Confirms Ablation 4's original conclusion (KEEP ENABLED) survives once the implementation genuinely works — a consistent win on every metric on both splits. |
| 2026-08-30 | (corrected) | **Corrected full 200-session exit, guaranteed path** (slate hedging OFF, LLM booster OFF) — supersedes every earlier guaranteed-path number in this log | **0.485** | **0.278095** | **6.975** | **0.406428** | Confirmed byte-identical across two repeated runs (determinism holds). Small (~0.6pp) drift from the earlier 0.40927 traced to the catalog embedding cache being regenerated from scratch during Phase 5.2's `.npy`→`.npz` migration — ordinary multi-threaded BLAS floating-point non-associativity between two separate encoding passes, not a control-flow bug (the hash-seed determinism fix remains fully intact and verified). **This is the current, accepted, true guaranteed-path figure — report this one.** |
| 2026-08-30 | (corrected) | **Corrected full 200-session exit, everything enabled** (LLM booster ON with the working fix; slate hedging stays OFF) | **0.505** | **0.312143** | **6.81** | **0.429943** | The optional ceiling with `ANTHROPIC_API_KEY` present. Still inert during official judging per D-LLM-TIER — 0.406428 remains the number to report as the expected competition score. |

| 2026-08-30 | (Phase 5.6, investigation) | **`tools/diagnose_buying_recall.py` (new): pool-vs-top-10 recall diagnostic, full 200 sessions, guaranteed path** — for each scenario, whether the hidden target ever reaches the fused/filtered candidate pool at all vs. reaches it but never the final top-10 vs. hits | boundary (n=10): 60.0% hit / 10.0% in-pool-not-top10 / 30.0% never-in-pool; browsing (n=80): 65.0% / 8.8% / 26.2%; **buying (n=80): 38.8% / 23.8% / 37.5%**; intent_override (n=30): 40.0% / 40.0% / 20.0% | — | — | — | First direct measurement (not inference) of *why* buying underperforms: a near-even split between a pure retrieval-recall failure (target never reaches the pool -- no amount of reranking fixes this) and a ranking failure (reaches the pool, never top-10), both far worse than browsing's numbers. This is the diagnostic none of the prior buying-track fix attempts (query nudge, weighted RRF, rerank-depth widening, slate hedging, lookahead) started from -- they all hypothesized a fix first. |
| 2026-08-30 | (Phase 5.6, investigation) | **Isolation test: same diagnostic, buying only, hard filter forced OFF** (`--no-hard-filter`, monkeypatches `route_retrieval_breadth` to always return `False`) | **byte-identical to the baseline buying row above**: 38.8% / 23.8% / 37.5%, no change at all | — | — | — | Toggling the *existing* hard filter changes nothing for buying. Root cause: `catalog.apply_hard_filters()` only ever restricts on `category`/`brand`/`budget_max` -- never on the disclosed hard-constraint attribute itself (material/color/style), and brand is structurally almost never disclosed (resolved-Q2) while budget is only sometimes present, so the "hard filter" reduces to "category filter" for most buying sessions regardless of the boolean. The intended "Buying-track precision" mechanism was effectively inert for its stated purpose. |
| 2026-08-30 | (Phase 5.6) | **`EXTENDED_HARD_FILTER_ATTRS`: also hard-restrict on disclosed material/color/style** (new `_by_material`/`_by_color`/`_by_style` catalog indices, same regex vocabulary `catalog._attributes_for()` already uses), training split (n=160) | ON: **0.476056**/0.5563/0.348936/6.3375 vs OFF (baseline): 0.441967/0.5188/0.319474/6.6625 | — | — | — | +7.7% TechnicalScore, +7.2% HitRate@10, +9.2% MRR, MTTC also improved (fewer turns). Real risk going in: material/color/style are single-regex-match, best-effort attributes, so a genuinely-matching product mentioning a different material/color first could be wrongly excluded (the same fragility resolved-Q7 found in `details`) -- measured anyway per this project's ablation discipline. |
| 2026-08-30 | (Phase 5.6) | **`EXTENDED_HARD_FILTER_ATTRS` confirmatory, validation split (n=40, held out, single-candidate run per `tune_strategy.py`'s enforced rule)** | ON: **0.445604**/0.5250/0.322014/6.6750 vs OFF (fresh same-run baseline): 0.388292/0.4500/0.304306/7.4000 | — | — | — | **+14.8% TechnicalScore, +16.7% HitRate@10, +5.8% MRR, MTTC improved** -- a clean win on BOTH splits, unlike weighted RRF/slate hedging/query nudge which all failed or reversed on this exact check. The single strongest, most robust result of this entire project. **ENABLED by default.** |
| 2026-08-30 | (Phase 5.6) | **Full 200-session guaranteed-path exit with `EXTENDED_HARD_FILTER_ATTRS` enabled — supersedes every earlier guaranteed-path number in this log** | **0.55** | **0.347643** | **6.405** | **0.471193** | **+15.9% TechnicalScore over the prior guaranteed-path figure (0.406428 → 0.471193).** Scenario breakdown: boundary 0.6/0.35/6.1, browsing 0.6625/0.446265/5.75, **buying 0.475/0.265412/6.5375 (up from ~0.36-0.39 across every prior measurement this session)**, intent_override 0.433333/0.303148/7.9. A repeat run to double-check determinism, and a separate optional-ceiling run (LLM booster + this fix, `ANTHROPIC_API_KEY` present), were both genuinely killed mid-flight by an external interrupt (not an evaluator failure -- confirmed via `kill -0` on the actual PIDs, not just a process-listing check) and produced no result; not re-attempted given the interrupt pattern. This doesn't weaken the headline number: it's a single completed run, but backed by two independent prior runs (training n=160, validation n=40) that already agree in direction and magnitude, and this project's hash-seed determinism fix is separately confirmed solid elsewhere in the log. **This is the new realistic expected competition score — report 0.471193, not 0.406428.** The optional LLM-booster ceiling on top of this fix hasn't been re-measured; treat the last-known ceiling delta (+6.4pp over the old guaranteed baseline) as stale, not as still applying on top of this new number, until re-measured. |

| 2026-08-30 | (Phase 5.6, post-fix verification) | `tools/diagnose_buying_recall.py` re-run, full 200 sessions, after `EXTENDED_HARD_FILTER_ATTRS` shipped | buying: 47.5% hit / 30.0% in-pool-not-top10 / 22.5% never-in-pool (was 38.8/23.8/37.5); intent_override: 43.3/46.7/10.0 (was 40.0/40.0/20.0, an unplanned side benefit); browsing/boundary essentially unchanged | — | — | — | Confirms the fix worked as diagnosed (never-in-pool nearly halved for buying) and cleanly surfaces the next bottleneck: a larger share of buying candidates now reach the pool but miss the top-10 — a ranking problem, not a recall problem, going forward. |

| 2026-08-30 | (validation-contamination remediation) | `tools/diagnose_buying_recall.py --split training --scenario buying`, before vs after `EXTENDED_HARD_FILTER_ATTRS`, n=64 training-only buying sessions | before: 42.2% hit@10 / 20.3% in-pool-not-top10 / 37.5% never-in-pool; after: 45.3% / 26.6% / 28.1% | — | — | — | Re-diagnosed using ONLY training-split hidden targets after a codex review found the original diagnostic touched all 200 sessions (including the 40-session validation holdout), contaminating the earlier "+14.8% validation" claim. The same recall-gap pattern replicates on training-only evidence (never-in-pool 37.5%→28.1%, proportionally similar to the original full-200 measurement) — confirms the fix proposal was legitimately motivated without needing the validation sessions, so the existing 40-session validation confirmation stands as a genuinely clean holdout result. See `wiki/03_design_log.md` for the full account. |

| 2026-08-30 | `1aff491` (post-bugfix re-verification) | **Full 200-session guaranteed-path exit after fixing the material-index and disclosed-value-normalization bugs — supersedes 0.471193** | **0.54** | **0.335365** | **6.56** | **0.45941** | **SUPERSEDED by the row below — this run was itself contaminated (see next entry).** Lower than the buggy-code result (0.471193), still meaningfully above the pre-Phase-5.6 baseline (0.406428). Scenario breakdown: boundary 0.6/0.373/6.1, browsing 0.7/0.474/5.51, buying 0.425/0.228/7.08, intent_override 0.4/0.241/8.13. Root cause: fixing the value-normalization bug means `EXTENDED_HARD_FILTER_ATTRS` now genuinely ENGAGES on disclosures the simulator labels (e.g. "color: red") that previously silently bypassed the filter entirely due to the bug — the earlier ablation's "+14.8% validation" result characterized a partially-disabled version of the mechanism, not the fully-active one. |
| 2026-08-30 | (LLM-key contamination found + fixed) | **`tools/run_eval.py`/`tools/tune_strategy.py` were silently exercising the optional LLM booster on every "guaranteed path" run** — `agent.py`'s `_build_llm_client()` calls `load_dotenv()`, whose default `override=False` reads the real `ANTHROPIC_API_KEY` from `.env` on disk regardless of the calling subprocess's own environment. Every ablation and headline number produced by these two tools today (including the `0.45941` row above) was therefore silently running the optional booster, not the guaranteed no-key path it claimed to measure. Fixed by setting `env["ANTHROPIC_API_KEY"] = ""` unconditionally in `tune_strategy.py`'s `run_one()` and by default in `run_eval.py` (new `--with-llm-booster` opt-out flag added for deliberate optional-ceiling runs). | — | — | — | — | Separately, the same day's `_build_bm25_and_metadata` performance bug (BM25F tokenized unconditionally, tripling catalog construction cost) was also fixed — both fixes are folded into the re-verification row below. **Open, unresolved question**: whether the earlier `0.471193` (Phase 5.6 exit) and `0.406428` figures were also contaminated by the same `.env` leak has NOT been checked — `.env`'s real key's presence/absence at the time of those specific runs is unverified. Treat every guaranteed-path number in this log dated before this fix with that caveat; only the row below is confirmed clean. |
| 2026-08-30 | `1aff491`+ (post-bugfix, LLM-key-excluded, `run_eval.py`) | **Full 200-session guaranteed-path exit, fully corrected (BM25F-laziness perf fix + LLM-key exclusion both applied) — the first genuinely-clean guaranteed-path measurement today; supersedes 0.45941, 0.471193, and 0.406428** | **0.53** | **0.294732** | **6.63** | **0.44082** | Scenario breakdown: boundary 0.6/0.3025/6.1, browsing 0.675/0.41755/5.7375, buying 0.425/0.190794/7.025, intent_override 0.4/0.241799/8.133333. Lower than 0.45941 on MRR specifically (0.294732 vs 0.335365, HitRate@10 unchanged 0.53 vs 0.54) — consistent with the LLM booster's known effect of improving ranking precision (MRR) more than raw recall (HitRate@10), which is exactly the leak this fix closes. **This is the current, accepted guaranteed-path figure — report 0.44082, not 0.45941 or 0.471193, until/unless the open contamination question above is resolved in either direction.** |

Add a row every time the local evaluator is run against a meaningfully different variant (not
every trivial commit). Link back to the relevant `02_design_decisions.md` entry when a metric
change is the direct result of a decision.
