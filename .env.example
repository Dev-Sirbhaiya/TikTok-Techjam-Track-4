# Copy to .env and fill in. Never commit the real .env (see .gitignore).
#
# Optional -- the shipped default path (cross-encoder reranker, no LLM) works with zero keys set
# per NFR-2 / D-LLM-TIER (implementation/06_DECISION_LOG.md). This is only for experiments that
# opt into an LLM-backed path (e.g. ranker.py's optional listwise booster, ad-hoc turn-by-turn
# probing) -- the organizer does not provide hosted model credentials.
ANTHROPIC_API_KEY=
